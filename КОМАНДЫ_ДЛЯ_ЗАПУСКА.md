# Команды для запуска и управления ботом

## 🚀 Запуск бота

После завершения установки и настройки `.env` файла:

```bash
# Запуск бота
sudo systemctl start clinic-bot

# Включение автозапуска при перезагрузке сервера
sudo systemctl enable clinic-bot

# Проверка статуса
sudo systemctl status clinic-bot
```

## 📊 Просмотр логов

### Логи systemd (рекомендуется):

```bash
# Последние 50 строк логов
sudo journalctl -u clinic-bot -n 50

# Логи в реальном времени
sudo journalctl -u clinic-bot -f

# Логи за сегодня
sudo journalctl -u clinic-bot --since today

# Логи за последний час
sudo journalctl -u clinic-bot --since "1 hour ago"
```

### Логи из файлов проекта:

```bash
# Основной лог
tail -f /opt/clinic-bot/logs/file.log

# Цветной лог
tail -f /opt/clinic-bot/logs/file_color.log

# JSON лог
tail -f /opt/clinic-bot/logs/file_json.log
```

## ⚙️ Управление ботом

```bash
# Запуск
sudo systemctl start clinic-bot

# Остановка
sudo systemctl stop clinic-bot

# Перезапуск (после изменения кода или конфигурации)
sudo systemctl restart clinic-bot

# Проверка статуса
sudo systemctl status clinic-bot

# Отключение автозапуска
sudo systemctl disable clinic-bot

# Включение автозапуска
sudo systemctl enable clinic-bot
```

## 🔄 Перезапуск после изменений

```bash
# Остановить бота
sudo systemctl stop clinic-bot

# Внести изменения в код или .env

# Запустить бота
sudo systemctl start clinic-bot

# Или просто перезапустить
sudo systemctl restart clinic-bot
```

## ✅ Проверка работы бота

1. **Проверьте статус:**
   ```bash
   sudo systemctl status clinic-bot
   ```
   Должно быть: `Active: active (running)`

2. **Проверьте логи на ошибки:**
   ```bash
   sudo journalctl -u clinic-bot -n 100
   ```

3. **Отправьте команду `/start` боту в Telegram**
   - Бот должен ответить приветственным сообщением
   - Должно появиться главное меню с кнопками

4. **Проверьте отправку заявок:**
   - Пройдите процесс записи на прием
   - Проверьте, что заявка появилась в указанном канале

## 🐛 Если бот не запускается

```bash
# 1. Проверьте логи
sudo journalctl -u clinic-bot -n 100

# 2. Проверьте .env файл
sudo cat /opt/clinic-bot/.env

# 3. Проверьте права доступа
sudo ls -la /opt/clinic-bot/
sudo chown -R clinicbot:clinicbot /opt/clinic-bot

# 4. Проверьте виртуальное окружение
sudo -u clinicbot /opt/clinic-bot/venv/bin/python --version

# 5. Попробуйте запустить вручную (для отладки)
sudo -u clinicbot /opt/clinic-bot/venv/bin/python /opt/clinic-bot/main.py
```

## 📝 Быстрые команды

```bash
# Статус
sudo systemctl status clinic-bot

# Перезапуск
sudo systemctl restart clinic-bot

# Логи (реальное время)
sudo journalctl -u clinic-bot -f

# Остановка
sudo systemctl stop clinic-bot

# Запуск
sudo systemctl start clinic-bot
```
