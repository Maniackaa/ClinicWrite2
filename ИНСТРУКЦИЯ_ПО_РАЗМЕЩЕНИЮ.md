# Инструкция по размещению Telegram бота ROYAL Clinic на сервере

## 📋 Содержание

1. [Требования](#требования)
2. [Подготовка сервера](#подготовка-сервера)
3. [Развертывание бота](#развертывание-бота)
4. [Настройка конфигурации](#настройка-конфигурации)
5. [Запуск и управление](#запуск-и-управление)
6. [Обновление бота](#обновление-бота)
7. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

- **ОС**: Ubuntu 20.04+ или Debian 11+ (рекомендуется)
- **Python**: версия 3.10 или выше (в Ubuntu 24.04 используется Python 3.12)
- **Права**: доступ root (sudo)
- **Интернет**: стабильное подключение для работы с Telegram API
- **Диск**: минимум 500 МБ свободного места

**Важно:** В Ubuntu 24.04 (Noble) по умолчанию установлен Python 3.12. Используйте команду `python3` вместо `python3.10`.

---

## 🚀 Подготовка сервера

### Шаг 1: Подключение к серверу

Подключитесь к серверу по SSH:

```bash
ssh username@your-server-ip
```

### Шаг 2: Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### Шаг 3: Установка необходимых пакетов

```bash
sudo apt install -y python3 python3-venv python3-pip git curl libpq-dev postgresql-client build-essential
```

**Важно:** Пакеты `libpq-dev` и `postgresql-client` необходимы для установки `psycopg2-binary` (драйвер PostgreSQL для Python).

Проверьте версию Python:

```bash
python3 --version
```

Должно быть: `Python 3.10.x` или выше (в Ubuntu 24.04 обычно Python 3.12)

**Примечание:** Если у вас Ubuntu 24.04 (Noble), используйте `python3` вместо `python3.10`. Система автоматически использует доступную версию Python 3.10+.

---

## 📦 Развертывание бота

### Шаг 1: Клонирование репозитория

```bash
# Переходим в директорию /opt
cd /opt

# Клонируем репозиторий
sudo git clone https://github.com/Maniackaa/ClinicWrite.git clinic-bot

# Переходим в директорию проекта
cd clinic-bot
```

### Шаг 2: Запуск скрипта развертывания

```bash
# Делаем скрипт исполняемым
sudo chmod +x deploy.sh

# Запускаем развертывание
sudo ./deploy.sh
```

Скрипт автоматически выполнит:
- ✅ Создание пользователя `clinicbot` для безопасности
- ✅ Создание директорий проекта
- ✅ Копирование файлов
- ✅ Создание виртуального окружения Python
- ✅ Установку всех зависимостей из `requirements.txt`
- ✅ Создание шаблона `.env` файла (если его нет)
- ✅ Установку и настройку systemd service

### Шаг 3: Проверка установки

Проверьте, что все файлы на месте:

```bash
ls -la /opt/clinic-bot/
```

Должны быть видны:
- `main.py`
- `handlers/`
- `keyboards/`
- `data/`
- `requirements.txt`
- и другие файлы проекта

---

## ⚙️ Настройка конфигурации

### Шаг 1: Создание файла .env

Если файл `.env` не был создан автоматически:

```bash
sudo cp /opt/clinic-bot/.env.example /opt/clinic-bot/.env
```

### Шаг 2: Редактирование .env файла

```bash
sudo nano /opt/clinic-bot/.env
```

Заполните следующие обязательные параметры:

```env
# Telegram Bot Settings
BOT_TOKEN='ваш_токен_бота'  # Получить у @BotFather в Telegram
ADMIN_IDS='ваш_telegram_id'  # Ваш Telegram ID (можно узнать у @userinfobot)

# PostgreSQL Database Settings (можно оставить пустым, если БД не используется)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
DB_HOST=
DB_PORT=5432

# Timezone
TIMEZONE="Europe/Moscow"

# Telegram Channel ID for notifications
GROUP_ID=ваш_id_канала  # ID канала для получения заявок (формат: -1001234567890)

# Price file ID (опционально, можно получить командой /get_price_file_id)
PRICE_FILE_ID=
```

### Шаг 3: Как получить необходимые данные

#### Получение токена бота:
1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

#### Получение вашего Telegram ID:
1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш ID (число) в `.env`

#### Получение ID канала:
1. Создайте канал в Telegram или используйте существующий
2. Добавьте бота в канал как администратора
3. Отправьте любое сообщение в канал
4. Перейдите по ссылке: `https://api.telegram.org/botВАШ_ТОКЕН/getUpdates`
5. Найдите `"chat":{"id":-1001234567890}` - это ID канала
6. Или используйте бота [@getidsbot](https://t.me/getidsbot)

### Шаг 4: Установка прав доступа

```bash
# Установка правильных прав на файлы
sudo chown -R clinicbot:clinicbot /opt/clinic-bot
sudo chmod 600 /opt/clinic-bot/.env
```

---

## ▶️ Запуск и управление

### Запуск бота

```bash
# Запуск бота
sudo systemctl start clinic-bot

# Включение автозапуска при перезагрузке сервера
sudo systemctl enable clinic-bot

# Проверка статуса
sudo systemctl status clinic-bot
```

Если все правильно, вы увидите:
```
● clinic-bot.service - ROYAL Clinic Telegram Bot
   Loaded: loaded (/etc/systemd/system/clinic-bot.service; enabled)
   Active: active (running) since ...
```

### Просмотр логов

#### Логи systemd (рекомендуется):

```bash
# Просмотр последних 50 строк логов
sudo journalctl -u clinic-bot -n 50

# Просмотр логов в реальном времени
sudo journalctl -u clinic-bot -f

# Просмотр логов за сегодня
sudo journalctl -u clinic-bot --since today
```

#### Логи из файлов проекта:

```bash
# Основной лог
tail -f /opt/clinic-bot/logs/file.log

# Цветной лог
tail -f /opt/clinic-bot/logs/file_color.log

# JSON лог
tail -f /opt/clinic-bot/logs/file_json.log
```

### Управление ботом

```bash
# Запуск
sudo systemctl start clinic-bot

# Остановка
sudo systemctl stop clinic-bot

# Перезапуск (после изменения кода или конфигурации)
sudo systemctl restart clinic-bot

# Проверка статуса
sudo systemctl status clinic-bot

# Отключение автозапуска
sudo systemctl disable clinic-bot

# Включение автозапуска
sudo systemctl enable clinic-bot
```

---

## 🔄 Обновление бота

### Обновление кода

```bash
# Переходим в директорию проекта
cd /opt/clinic-bot

# Останавливаем бота
sudo systemctl stop clinic-bot

# Обновляем код из репозитория
sudo git pull

# Обновляем зависимости (если requirements.txt изменился)
sudo -u clinicbot /opt/clinic-bot/venv/bin/pip install -r requirements.txt --upgrade

# Запускаем бота
sudo systemctl start clinic-bot

# Проверяем статус
sudo systemctl status clinic-bot
```

### Обновление только зависимостей

```bash
sudo systemctl stop clinic-bot
sudo -u clinicbot /opt/clinic-bot/venv/bin/pip install -r /opt/clinic-bot/requirements.txt --upgrade
sudo systemctl start clinic-bot
```

---

## 🔍 Решение проблем

### Бот не запускается

1. **Проверьте логи:**
   ```bash
   sudo journalctl -u clinic-bot -n 100
   ```

2. **Проверьте файл .env:**
   ```bash
   sudo cat /opt/clinic-bot/.env
   ```
   Убедитесь, что все обязательные поля заполнены правильно.

3. **Проверьте права доступа:**
   ```bash
   sudo ls -la /opt/clinic-bot/
   sudo chown -R clinicbot:clinicbot /opt/clinic-bot
   ```

4. **Проверьте виртуальное окружение:**
   ```bash
   sudo -u clinicbot /opt/clinic-bot/venv/bin/python --version
   ```

### Ошибка "Module not found"

```bash
# Переустановите зависимости
sudo systemctl stop clinic-bot
sudo -u clinicbot /opt/clinic-bot/venv/bin/pip install -r /opt/clinic-bot/requirements.txt
sudo systemctl start clinic-bot
```

### Ошибка "Permission denied"

```bash
# Исправьте права доступа
sudo chown -R clinicbot:clinicbot /opt/clinic-bot
sudo chmod 600 /opt/clinic-bot/.env
```

### Бот не отвечает на сообщения

1. Проверьте, что бот запущен:
   ```bash
   sudo systemctl status clinic-bot
   ```

2. Проверьте токен в .env файле

3. Проверьте логи на наличие ошибок:
   ```bash
   sudo journalctl -u clinic-bot -f
   ```

### Ошибка подключения к Telegram API

1. Проверьте интернет-соединение сервера:
   ```bash
   ping api.telegram.org
   ```

2. Проверьте файрвол:
   ```bash
   sudo ufw status
   ```

### Бот падает и перезапускается

1. Проверьте логи для поиска ошибок:
   ```bash
   sudo journalctl -u clinic-bot -n 200
   ```

2. Проверьте использование ресурсов:
   ```bash
   top
   ```

3. Проверьте место на диске:
   ```bash
   df -h
   ```

---

## 📊 Мониторинг

### Проверка использования ресурсов

```bash
# Использование памяти и CPU
top -p $(pgrep -f "clinic-bot")

# Использование диска
du -sh /opt/clinic-bot/
```

### Автоматический мониторинг (опционально)

Можно настроить мониторинг через systemd:

```bash
# Настройка автоматического перезапуска при сбоях
sudo systemctl edit clinic-bot
```

Добавьте:
```ini
[Service]
Restart=always
RestartSec=10
StartLimitInterval=200
StartLimitBurst=5
```

---

## 🔐 Безопасность

### Рекомендации по безопасности:

1. **Файл .env должен быть защищен:**
   ```bash
   sudo chmod 600 /opt/clinic-bot/.env
   ```

2. **Не коммитьте .env в git** (уже в .gitignore)

3. **Регулярно обновляйте зависимости:**
   ```bash
   sudo -u clinicbot /opt/clinic-bot/venv/bin/pip list --outdated
   ```

4. **Используйте файрвол:**
   ```bash
   sudo ufw enable
   sudo ufw allow ssh
   ```

5. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `sudo journalctl -u clinic-bot -f`
2. Проверьте конфигурацию: `sudo cat /opt/clinic-bot/.env`
3. Проверьте статус: `sudo systemctl status clinic-bot`

---

## ✅ Чек-лист развертывания

- [ ] Сервер подготовлен (Python 3.10+, git установлен)
- [ ] Репозиторий клонирован в `/opt/clinic-bot`
- [ ] Скрипт `deploy.sh` выполнен успешно
- [ ] Файл `.env` создан и заполнен
- [ ] Токен бота получен и добавлен в `.env`
- [ ] Telegram ID администратора добавлен в `.env`
- [ ] ID канала добавлен в `.env`
- [ ] Права доступа установлены правильно
- [ ] Бот запущен: `sudo systemctl start clinic-bot`
- [ ] Автозапуск включен: `sudo systemctl enable clinic-bot`
- [ ] Статус проверен: `sudo systemctl status clinic-bot`
- [ ] Логи проверены на наличие ошибок
- [ ] Бот отвечает на команду `/start` в Telegram

---

**Готово! Бот должен работать и автоматически запускаться при перезагрузке сервера.** 🎉
