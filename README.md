# Бот "Объединяем компетенции"

Telegram-бот для проекта "Объединяем компетенции" - профессиональной площадки для врачей.

## Функционал

### Главное меню
После команды `/start` пользователь видит приветственное сообщение и меню с кнопками:
- 🔸 Наш проект
- 🔸 Регистрация на 14 марта
- 🔸 Подписаться на канал "Объединяем компетенции"
- 🔸 Перейти на сайт ROYAL CLINIC
- 🔸 Подписаться на канал ROYAL CLINIC

### Раздел "Наш проект"
Содержит описание проекта и две кнопки:
1. **Архив прошедших мероприятий** - отправляет 2 видео (по video_id)
2. **Отзывы участников** - отправляет 2 видео (по video_id)

### Регистрация на конференцию
Многошаговая форма регистрации:
1. Ввод ФИО
2. Ввод телефона (можно поделиться контактом)
3. Ввод email

После успешной регистрации:
- Заявка отправляется в канал (GROUP_ID из .env)
- Пользователю приходит подтверждение с кликабельной ссылкой на канал

## Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd ClinicWriteBot2
```

### 2. Установка зависимостей
```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac

pip install -r requirements.txt
```

### 3. Настройка переменных окружения
Создайте файл `.env` на основе `.env.example`:
```env
BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=123456789,987654321
GROUP_ID=-1001234567890
TIMEZONE=Europe/Moscow
```

## Настройка

### Добавление видео
Для добавления видео используйте админ-команду `/get_video_id`:
1. Отправьте команду `/get_video_id` боту
2. Отправьте видео боту
3. Скопируйте полученный `file_id`
4. Вставьте его в `data/project_data.py` в массивы:
   - `ARCHIVE_VIDEOS` - для архива мероприятий
   - `REVIEWS_VIDEOS` - для отзывов участников

Пример:
```python
ARCHIVE_VIDEOS = [
    "BAACAgIAAxkBAAID2mmTU8fA6HsgTgJz_7BJlP3fJIP6AAKhoAACfeSZSH8FC8AnwIgNOgQ",
    "BAACAgIAAxkBAAID9mmTY1iR3v4iGK_xBixg_CX--4V8AAL3oQACfeSZSLmyYs2pRTBROgQ",
]
```

## Запуск

```bash
python main.py
```

## Структура проекта

```
├── main.py                  # Точка входа
├── handlers/
│   ├── user_handlers.py     # Обработчики пользователей
│   ├── action_handlers.py   # Обработчики событий канала/группы
│   └── states.py            # FSM состояния для регистрации
├── keyboards/
│   └── keyboards.py         # Генерация клавиатур
├── data/
│   └── project_data.py      # Тексты и video_id
└── config_data/
    └── conf.py              # Конфигурация
```

## Особенности

- Использование `video_id` для быстрой отправки видео без загрузки файлов
- Валидация всех полей формы регистрации
- Логирование всех действий
- Обработка ошибок с fallback-механизмами

## Загрузка на GitHub

Если репозиторий еще не создан на GitHub:

1. Создайте новый репозиторий на GitHub (через веб-интерфейс)

2. Добавьте remote и запушьте код:
```bash
git remote add origin https://github.com/ваш-username/ClinicWriteBot2.git
git branch -M main
git push -u origin main
```

Или если используете SSH:
```bash
git remote add origin git@github.com:ваш-username/ClinicWriteBot2.git
git branch -M main
git push -u origin main
```

